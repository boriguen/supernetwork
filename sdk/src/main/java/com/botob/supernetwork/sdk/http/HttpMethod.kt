package com.botob.supernetwork.sdk.http

/**
 * [HttpMethod] is the enum class listing the supported HTTP methods.
 */
enum class HttpMethod {
    GET,
    POST,
    HEAD,
    OPTIONS,
    PUT,
    DELETE,
    TRACE
}